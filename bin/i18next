import i18n from 'i18next';

i18n.init({
  resources: {
    en: {
      translation: {
        "valid_url_required": "URL must be valid",
        "rss_link_required": "URL must be an RSS feed link",
        "url_already_added": "URL is already added"
        // Остальные английские переводы...
      }
    },
    ru: {
      translation: {
        "valid_url_required": "URL должен быть валидным",
        "rss_link_required": "URL должен быть ссылкой на RSS-поток",
        "url_already_added": "URL уже добавлен"
        // Остальные русские переводы...
      }
    }
    // Дополнительные языковые ресурсы...
  },
  lng: "en", // язык по умолчанию
  fallbackLng: "en",
  interpolation: {
    escapeValue: false
  }
});

export default i18n;
